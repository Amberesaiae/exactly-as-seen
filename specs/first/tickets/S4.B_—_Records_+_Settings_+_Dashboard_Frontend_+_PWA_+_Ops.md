# S4.B — Records + Settings + Dashboard Frontend + PWA + Ops

## Goal

Rebuild the three remaining frontend pages, add PWA support, fix the mobile nav, and complete ops hardening. This is the final ticket — the platform is feature-complete after this.

## Spec Reference

spec:3a092065-e868-4799-849c-f707a0553261/b7f8a421-4897-4bc3-bfc4-850e84f63a24 — Sprint 4 §4.4–4.8
file:specs/08_RECORDS.md, file:specs/09_MAIN_DASHBOARD.md, file:specs/10_SETTINGS.md

## Dependencies

- S4.A (all three backend modules must exist)

## Records Page Rebuild

Replace file:artifacts/lampfarms/src/pages/Records.tsx (currently a clean stub) with the full 4-tab layout.

| Tab | Data source | Key components |
| --- | --- | --- |
| Overview | `GET /api/v1/records/overview` | KPI cards (active batches, birds on farm, avg mortality, net profit masked) |
| Batches | `GET /api/v1/records/batches` + detail + compare | Batch list table; detail drawer; compare (2–4 batches) with best-performer highlight |
| Health | `GET /api/v1/records/health/summary` | Task completion rates, vaccination compliance, withdrawal history |
| Finance | `GET /api/v1/records/finance/summary` | Proxies Finance P&L with same privacy rules |

- PDF/CSV export buttons wired to server-rendered endpoints — no client-side generation
- No client-side aggregation anywhere
- No client-side cost masking — server controls what is returned

## Settings Page Rebuild

Replace file:artifacts/lampfarms/src/pages/SettingsPage.tsx (980 lines) with 5-tab layout per file:specs/10_SETTINGS.md §12.

| Tab | Key changes vs current |
| --- | --- |
| Profile | Remove password change dialog and `PasswordStrengthIndicator`; show OIDC note |
| Farm | Add `waterSourceChlorinated` toggle; houses CRUD wired to `/settings/houses` |
| Preferences | Currency: GHS/NGN only (remove USD/EUR/GBP); PIN setup for cost privacy |
| Market Prices | New tab — L3 config overrides table with add/delete |
| Data | Export buttons + account deletion with confirm phrase |

No file over ~250 lines. Each tab is a separate component.

## Dashboard Page Rebuild

Replace file:artifacts/lampfarms/src/pages/Dashboard.tsx (424 lines, supabase shim) with spec-compliant version consuming `GET /api/v1/dashboard/overview`.

- Use Dexie `dashboard_cache` table for offline rendering (already in schema from Sprint 0.3)
- Cache stores exactly what the server returned — masked values remain masked in cache
- `costPrivacyEnabled` toggle calls `PATCH /api/v1/settings/preferences` — no client-side masking
- Batch tiles show duck-type sub-badge when `duck_type` present
- Withdrawal ring on tiles when `in_withdrawal = true`
- Mortality modal wired to `POST /api/v1/batches/:id/mortality` (not supabase shim)

## Mobile Nav Fix

file:artifacts/lampfarms/src/components/MobileNav.tsx: change to exactly 5 primary items — Home, Batches, Feed, Water-Health, Settings. Remove "More" sheet. Eggs, Finance, Stock, Records accessible via sidebar.

file:artifacts/lampfarms/src/components/AppSidebar.tsx: change label from "Water & Health" to "Water-Health".

## PWA

- Add Workbox `injectManifest` service worker
- PWA install banner component
- Offline routing for known views
- Sync-on-reconnect surface: `online` event → flush outbox

## Ops Hardening

- Rate limiting middleware on auth endpoints and all write endpoints (return `429` on abuse)
- CORS allowlist for known origins (configurable via env)
- Extend file:lib/api-spec/openapi.yaml to cover all domain endpoints (currently auth-only)
- WCAG 2.1 AA audit on all primary pages

## Acceptance Criteria

- Records page has no client-side aggregation; all data from `/api/v1/records/*`
- Records compare shows best-performer highlight per metric direction
- Settings page has 5 tabs; no password change UI; currency options are GHS and NGN only
- Dashboard reads from `dashboard_cache` when offline
- Dashboard privacy matches Finance/Records server-side masking rules
- Dashboard mortality modal calls `/api/v1/batches/:id/mortality` (not supabase shim)
- Mobile nav shows exactly 5 items: Home, Batches, Feed, Water-Health, Settings
- AppSidebar label is "Water-Health"
- PWA installs on Chrome desktop and mobile
- Rate limiter returns `429` on abusive traffic
- OpenAPI spec covers all domain endpoints
- `bun run typecheck` passes