# Finding F-F-001

| Field | Value |
|-------|-------|
| ID | F-F-001 |
| Date | 2026-07-17 |
| System pack | `02-systems/F-stock` |
| Severity | P0 |
| Class | dual-writer |
| WRITER_MAP | **K3** |

## Observation

`useStockData.recordTransaction` for `type === 'purchase'` called `stock_purchase` RPC, but on non-ok / incomplete success path fell through to client inserts: `stock_transactions` + `stock_lots` + `stock_items` update + `expenses` — partial money risk.

## Expected

Purchase only via `stock_purchase`; RPC fail → toast + return; zero client multi-write.

## Curation

- [x] FIX — purchase branch early-returns on success/fail; fallthrough removed
