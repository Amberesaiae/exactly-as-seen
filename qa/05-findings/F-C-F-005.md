# Finding F-C-F-005

| Field | Value |
|-------|-------|
| ID | F-C-F-005 |
| Date | 2026-07-17 |
| System pack | `02-systems/C-today-ops-feed` |
| Severity | P3 |
| Class | surface / defense-in-depth |

## Observation

RLS still allows authenticated farm owners to `INSERT`/`UPDATE` `feed_logs` directly (`20260711000000_contract_alignment.sql`). After K1 FE kill, clients no longer call insert for day feed, but any other client or bug can still dual-write.

## Expected (hard sole writer)

Only `confirm_day_feed` SECURITY DEFINER writes `feed_logs` (or narrow service role). Client role: SELECT only.

## Curation

- [ ] FIX
- [x] DEFER — migration to revoke client INSERT/UPDATE on `feed_logs` (and document RPC-only). Out of minimal FE kill; schedule with spine hardening pass.
- [ ] ACCEPT

Residual: FE sole path; DB still permits dual.
