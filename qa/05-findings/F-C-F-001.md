# Finding F-C-F-001

| Field | Value |
|-------|-------|
| ID | F-C-F-001 |
| Date | 2026-07-17 |
| System pack | `02-systems/C-today-ops-feed` |
| Journey | C Day feed |
| Severity | P0 |
| Class | dual-writer |
| WRITER_MAP | **K1** |

## Observation

Static inventory of `src/hooks/feed/useFeedData.ts`:`confirmDayFeed`: on `rpcErr` after `supabase.rpc('confirm_day_feed')`, client performed:

1. `supabase.from('feed_logs').insert(...)`
2. `autoDeductStock(...)` (FIFO + stock qty)
3. optional `autoCreateExpense(...)`

Non-atomic partial side-effect set; violates one intent → one writer → one side-effect set. Explicit ban: no client multi-write rescue after RPC fail.

`useHealthData` feeding path already threw on `rpcErr` (fail-closed).

## Expected (canonical)

RPC fail → error toast; **zero** client writes to `feed_logs` / stock / expenses for that intent.

## Actual (pre-fix)

Client rescue multi-write; possible log without stock/expense or mixed partials.

## Evidence

- Code: pre-fix `useFeedData.ts` ~L232–270 (removed 2026-07-17)
- WRITER_MAP K1 row

## Curation

- [x] FIX applied (session 2026-07-17)
- [ ] DEFER
- [ ] ACCEPT residual

Decision: **FIX** — on `rpcErr`, toast + return only. No `feed_logs.insert` / synergy on confirm path.
