# Finding F-C-F-002

| Field | Value |
|-------|-------|
| ID | F-C-F-002 |
| Date | 2026-07-17 |
| System pack | `02-systems/C-today-ops-feed` |
| Journey | C Day feed |
| Severity | P0 |
| Class | offline |
| WRITER_MAP | **K2** |

## Observation

Feed Lab offline branch queued **only** raw table write:

`queueWrite('feed_logs', 'insert', …)`

Flush would insert log without stock/expense. Health path already used `queueRpc('confirm_day_feed', args)` (OFFLINE-OK). WRITER_MAP listed both files under K2; Health was already correct.

## Expected (canonical)

Offline day feed = `queueRpc('confirm_day_feed', fullArgs)` so flush runs full spine semantics.

## Actual (pre-fix)

Feed Lab offline ≠ online (log-only outbox).

## Evidence

- Pre-fix `useFeedData.ts` ~L204–216
- `src/lib/sync.ts` flush supports `rpc:confirm_day_feed`

## Curation

- [x] FIX applied (session 2026-07-17)
- [ ] DEFER
- [ ] ACCEPT residual

Decision: **FIX** — offline uses `queueRpc('confirm_day_feed', rpcArgs)`. Grep clean: no `queueWrite('feed_logs'` in `src/`.
