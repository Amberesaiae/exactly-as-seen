# Finding F-C-F-003

| Field | Value |
|-------|-------|
| ID | F-C-F-003 |
| Date | 2026-07-17 |
| System pack | `02-systems/C-today-ops-feed` |
| Journey | C Day feed (Book now) |
| Severity | P1 |
| Class | ledger-lie |

## Observation

`useHealthData` Book now after flexible day feed re-called `confirm_day_feed` with `p_ledger: true`. Feed already logged → RPC returns `{ ok, already_logged: true }` **without** Postgres error → client set `rpcOk = !bookErr` true → success toast **without** writing expense. Fallback `autoCreateExpense` only ran when `bookErr` was set.

Feed Lab Book now already used `autoCreateExpense` with `source_ref …:book` (KEEP).

## Expected (canonical)

Book now = manual expense only (`auto:feed` + `:book` source_ref). Never re-run day-feed intent after log exists.

## Actual (pre-fix)

False success; no expense row.

## Curation

- [x] FIX applied (session 2026-07-17)
- [ ] DEFER
- [ ] ACCEPT residual

Decision: **FIX** — Health Book now matches Feed Lab: `autoCreateExpense` only.
