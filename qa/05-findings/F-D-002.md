# Finding F-D-002

| Field | Value |
|-------|-------|
| ID | F-D-002 |
| Date | 2026-07-17 |
| System pack | `02-systems/D-care-health` |
| Severity | P0 |
| Class | dual-writer / ledger-lie risk |
| WRITER_MAP | **K6** |

## Observation

`runPostCompletionSideEffects` after successful `complete_health_task` called `autoDeductStock` + `autoCreateExpense` for intensive systems. RPC already inserts expense when `p_cost_pesewas > 0` with `source_ref = task_id` — client re-ledger risk (or duplicate unique) + non-atomic stock.

## Expected

RPC owns intensive ledger. FE only Book now (`:book` ref) on flexible.

## Curation

- [x] FIX — post-effects = `seedPostVaccinationSupplements` only
